# Core dependencies
numpy>=1.26.0
scipy>=1.11.0
pandas>=2.1.0

# Database
psycopg2-binary>=2.9.9
asyncpg>=0.29.0  # Async PostgreSQL driver
sqlalchemy>=2.0.23
alembic>=1.13.0

# Storage
boto3>=1.34.0  # For S3/Tigris
aioboto3>=12.0.0  # Async version of boto3
soundfile>=0.12.1  # For audio I/O
# Note: FLAC compression is handled by soundfile, no need for pyFLAC

# Distributed coordination
redis>=5.0.1

# API
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.5.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0

# Audio processing
librosa>=0.10.1  # For audio analysis
scikit-learn>=1.3.0  # For ML features
matplotlib>=3.7.0  # For waterfall plots and visualizations

# Async
aioredis>=2.0.1
# asyncio is built-in to Python 3.7+, no need to install
httpx>=0.26.0  # Async HTTP client
websockets>=12.0  # WebSocket client for WebSDR connections
websocket-client>=1.7.0  # For KiwiSDR

# Web scraping for SDR registry sync
beautifulsoup4>=4.12.0
requests>=2.31.0

# Time & Date
python-dateutil>=2.8.2
pytz>=2023.3

# Logging & Monitoring
python-json-logger>=2.0.7
rich>=13.7.0  # Terminal UI
psutil>=5.9.0  # Resource monitoring for auto-scaling
prometheus-client>=0.19.0  # Prometheus metrics export

# External services
pyephem>=9.99  # Ephemeris calculations

# Utilities
python-dotenv>=1.0.0
pyyaml>=6.0.1
click>=8.1.7  # CLI tools

# Linting (dev only, but included for consistency)
ruff>=0.1.0

# Note: kiwiclient will be installed from GitHub in Dockerfile
# Note: FT8/WSPR decoders would need separate binary installation